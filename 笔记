即时SQL（普通语句）
‌编译过程‌
每条SQL语句首次执行时需完整经历：

‌词法/语法解析‌：将SQL文本拆解为语法树‌25
‌语义验证‌：检查表/列是否存在、权限等‌5
‌优化器生成执行计划‌：基于统计信息选择索引、连接顺序等‌25
‌编译为二进制指令‌：最终转换为数据库引擎可执行的底层操作码‌27
‌执行特点‌

每次执行均需重新编译，生成独立的执行计划‌17
例如连续执行3条DELETE FROM emp WHERE id=1/2/3会编译3次‌1
二、预编译SQL（Prepared Statements）
‌编译结果‌

将SQL中的变量替换为占位符（如?或#{id}），生成参数化模板‌14
编译后的模板缓存在内存池（数据库缓冲池），保留执行计划‌47
‌执行特点‌

‌一次编译多次运行‌：后续仅需传递参数值，跳过解析/优化阶段‌14
例如DELETE FROM emp WHERE id=?只需编译1次，通过不同参数重复执行‌17
支持防止SQL注入，因参数值不会作为语法解析‌17
